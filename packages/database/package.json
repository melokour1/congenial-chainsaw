{
  "name": "@laxvaletcare/database",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate --schema=schema.prisma",
    "migrate:dev": "prisma migrate dev --schema=schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "prisma": "^5.20.0"
  }
}
