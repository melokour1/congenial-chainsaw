/// <reference types="expo/types" />

// NOTE: this triple-slash directive lets TypeScript pick up the ambient
// types Expo generates (expo-router route typing, etc). It's committed here
// so the project type-checks before the first `expo start` run generates it.
